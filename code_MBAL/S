def S(Ro, L, Zsr, Tsr_C):
    """
    Расчёт экспоненциального сопротивления потоку газа.

    Параметры:
    - Ro (float): относительная плотность газа
    - L (float): длина вертикального участка, м
    - Zsr (float): средний коэффициент сжимаемости
    - Tsr_C (float): средняя температура, °C

    Возвращает:
    - S (float): безразмерный параметр сопротивления
    """
    Tsr_K = Tsr_C + 273.15
    return 0.03419 * Ro * L / (Zsr * Tsr_K)
