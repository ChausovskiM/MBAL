def Mbal_A(Swi, Cf, Cw):
    """
    Расчёт приведённой сжимаемости системы.

    Параметры:
    - Swi (float): начальная водонасыщенность (д.ед.)
    - Cf (float): сжимаемость породы (1/МПа)
    - Cw (float): сжимаемость воды (1/МПа)

    Возвращает:
    - Ca (float): приведённая сжимаемость системы (1/МПа)
    """
    if (1 - Swi) <= 0:
        return 0.0
    return (Swi * Cw + Cf) / (1 - Swi)
