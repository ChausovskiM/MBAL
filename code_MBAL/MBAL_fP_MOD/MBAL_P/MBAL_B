def Mbal_B(t):
    """
    Пересчёт стандартного давления по температуре.

    Параметры:
    - t (float): температура пласта, °C

    Возвращает:
    - B (float): коэффициент пересчёта давления, МПа
    """
    return 0.101325 * (t + 273.15) / (20 + 273.15)
