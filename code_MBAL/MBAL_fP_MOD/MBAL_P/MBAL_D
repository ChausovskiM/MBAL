from Mbal_A import Mbal_A

def Mbal_D(Swi, Cf, Cw, Z, P):
    """
    Расчёт коэффициента B в уравнении материального баланса.

    Параметры:
    - Swi (float): начальная водонасыщенность
    - Cf (float): сжимаемость породы, 1/МПа
    - Cw (float): сжимаемость воды, 1/МПа
    - Z (float): начальный коэффициент сжимаемости
    - P (float): давление, МПа

    Возвращает:
    - B (float): коэффициент B
    """
    return -Z * Mbal_A(Swi, Cf, Cw) / P
