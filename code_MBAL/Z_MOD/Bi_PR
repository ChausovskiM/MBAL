def Bi_PR(P, T, i, Pc_i, Tc_i):
    """
    Расчёт индивидуального параметра Bi для компонента i
    в уравнении Пинг–Робинсона.

    Параметры:
    - P (float): давление, Па
    - T (float): температура, K
    - i (int): индекс компонента (информационный)
    - Pc_i (float): критическое давление, МПа
    - Tc_i (float): критическая температура, K

    Возвращает:
    - Bi (float): параметр объёмного вклада, безразмерный
    """
    Pkr = Pc_i * 1e6
    Tkr = Tc_i
    Ppr = P / Pkr
    Tpr = T / Tkr

    return 0.077796074 * Ppr / Tpr