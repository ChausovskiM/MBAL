def Cp(Gg, Go, Cpg, Cpo):
    """
    Расчёт средней удельной теплоёмкости смеси.

    Параметры:
    - Gg (float): массовый расход газа, кг/ч
    - Go (float): массовый расход нефти, кг/ч
    - Cpg (float): удельная теплоёмкость газа, Дж/(кг·К)
    - Cpo (float): удельная теплоёмкость нефти, Дж/(кг·К)

    Возвращает:
    - Cp (float): удельная теплоёмкость смеси, Дж/(кг·К)
    """
    total = Gg + Go
    if total == 0:
        return 0.0

    return (Gg / total) * Cpg + (Go / total) * Cpo
