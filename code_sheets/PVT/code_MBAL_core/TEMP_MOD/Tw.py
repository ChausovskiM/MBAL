def Tw(D1, D2):
    """
    Расчёт временного интервала между двумя датами в часах.

    Параметры:
    - D1 (datetime): начальная дата
    - D2 (datetime): конечная дата

    Возвращает:
    - Tw (float): интервал времени, часов
    """
    delta = D2 - D1
    return delta.total_seconds() / 3600
