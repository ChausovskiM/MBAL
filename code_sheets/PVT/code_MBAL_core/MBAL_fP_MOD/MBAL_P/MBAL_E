def Mbal_E(Qi, Qngz, Zi):
    """
    Расчёт правой части уравнения материального баланса.

    Параметры:
    - Qi (float): накопленная добыча
    - Qngz (float): начальные запасы газа
    - Zi (float): коэффициент сжимаемости на текущем шаге

    Возвращает:
    - E (float): правое выражение уравнения баланса
    """
    return (1 - Qi / Qngz) * Zi
