from Mbal_A import Mbal_A
from Mbal_B import Mbal_B

def Mbal_C(Swi, Cf, Cw, Z, P, t, We, Qngz):
    """
    Расчёт коэффициента A в уравнении материального баланса.

    Параметры:
    - Swi, Cf, Cw: параметры сжимаемости
    - Z (float): коэффициент сверхсжимаемости
    - P (float): начальное давление, МПа
    - t (float): температура, °C
    - We (float): водонапорный отклик
    - Qngz (float): начальные запасы газа

    Возвращает:
    - A (float): коэффициент A
    """
    term1 = Z / P
    term2 = Z * Mbal_A(Swi, Cf, Cw)
    term3 = We / (Qngz * Mbal_B(t))
    return term1 - term2 - term3
